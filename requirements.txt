# Requirements for running proagent_demo.py
# This includes only the packages needed for the demo

# Core dependencies
numpy
scipy
gym==0.22
pygame
tqdm

# OpenAI API for LLM calls
openai
jsonschema

# Project itself (editable install)
-e .
